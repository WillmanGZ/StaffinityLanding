---
interface VacancyInfo {
  icon: string;
  label: string;
  value: string;
}

interface Vacancy {
  badge?: string;
  title: string;
  info: VacancyInfo[];
  description: string;
  buttonText?: string;
}

interface Props {
  sectionTitle?: string;
  sectionSubtitle?: string;
  vacancies?: Vacancy[];
}

const {
  sectionTitle = "Vacantes Disponibles",
  sectionSubtitle = "Únete a nuestro equipo y construye el futuro de la gestión de capital humano",
  vacancies = [
    {
      badge: "Urgente",
      title: "Backend Developer .NET",
      info: [
        { icon: "📍", label: "Ubicación", value: "Remoto / Barranquilla" },
        { icon: "💼", label: "Tipo", value: "Tiempo Completo" },
        { icon: "🏢", label: "Departamento", value: "Engineering" },
      ],
      description:
        "Desarrolla microservicios robustos con C# y .NET para el módulo de Personal. Trabaja con arquitectura hexagonal, Entity Framework y SQL Server.",
      buttonText: "Aplicar Ahora",
    },
    {
      badge: "Nuevo",
      title: "Backend Developer Java",
      info: [
        { icon: "📍", label: "Ubicación", value: "Híbrido" },
        { icon: "💼", label: "Tipo", value: "Tiempo Completo" },
        { icon: "🏢", label: "Departamento", value: "Engineering" },
      ],
      description:
        "Construye servicios escalables con Spring Boot y PostgreSQL para el sistema de reclutamiento. Experiencia con APIs REST y arquitectura limpia.",
      buttonText: "Aplicar Ahora",
    },
    {
      title: "Frontend Developer Next.js",
      info: [
        { icon: "📍", label: "Ubicación", value: "Remoto" },
        { icon: "💼", label: "Tipo", value: "Tiempo Completo" },
        { icon: "🏢", label: "Departamento", value: "Engineering" },
      ],
      description:
        "Desarrolla interfaces modernas y responsivas con Next.js y TypeScript para los portales de HexaLink.",
      buttonText: "Aplicar Ahora",
    },
  ],
} = Astro.props;
---

<section id="vacantes" class="vacancies">
  <div class="content-container">
    <div class="section-title">
      <h2>{sectionTitle}</h2>
      <p>{sectionSubtitle}</p>
    </div>
    <div class="vacancies-grid">
      {
        vacancies.map((vacancy) => (
          <div class="vacancy-card">
            {vacancy.badge && (
              <span class="vacancy-badge">{vacancy.badge}</span>
            )}
            <h3>{vacancy.title}</h3>
            <div class="vacancy-info">
              {vacancy.info.map((item) => (
                <div class="vacancy-info-item">
                  <span>{item.icon}</span>
                  <span>
                    <strong>{item.label}:</strong> {item.value}
                  </span>
                </div>
              ))}
            </div>
            <p>{vacancy.description}</p>
            <button class="btn-primary vacancy-btn">
              {vacancy.buttonText || "Aplicar Ahora"}
            </button>
          </div>
        ))
      }
    </div>
  </div>
</section>
