---
interface AboutCard {
  icon: string;
  title: string;
  description: string;
}

interface Props {
  sectionTitle?: string;
  sectionSubtitle?: string;
  cards?: AboutCard[];
}

const {
  sectionTitle = "About HexaLink",
  sectionSubtitle = "We transform human capital management with cutting-edge technology",
  cards = [
    {
      icon: "🎯",
      title: "Mission",
      description:
        "Simplify and automate human resources management processes through an integrated platform that improves operational efficiency and employee experience.",
    },
    {
      icon: "🚀",
      title: "Vision",
      description:
        "To be the leading platform in human capital solutions, using modern microservices architectures to deliver scalability, flexibility, and technological excellence.",
    },
    {
      icon: "💎",
      title: "Values",
      description:
        "Continuous innovation, process transparency, operational efficiency, and commitment to excellence in every interaction with our users.",
    },
  ],
} = Astro.props;
---

<section id="about" class="about">
  <div class="content-container">
    <div class="section-title">
      <h2>{sectionTitle}</h2>
      <p>{sectionSubtitle}</p>
    </div>
    <div class="about-grid">
      {
        cards.map((card) => (
          <div class="about-card">
            <div class="about-icon">{card.icon}</div>
            <h3>{card.title}</h3>
            <p>{card.description}</p>
          </div>
        ))
      }
    </div>
  </div>
</section>
