---
interface AboutCard {
  icon: string;
  title: string;
  description: string;
}

interface Props {
  sectionTitle?: string;
  sectionSubtitle?: string;
  cards?: AboutCard[];
}

const {
  sectionTitle = "Acerca de HexaLink",
  sectionSubtitle = "Transformamos la gestión de capital humano con tecnología de vanguardia",
  cards = [
    {
      icon: "🎯",
      title: "Misión",
      description:
        "Simplificar y automatizar los procesos de gestión de recursos humanos mediante una plataforma integrada que mejora la eficiencia operativa y la experiencia del empleado.",
    },
    {
      icon: "🚀",
      title: "Visión",
      description:
        "Ser la plataforma líder en soluciones de capital humano, utilizando arquitecturas modernas de microservicios para ofrecer escalabilidad, flexibilidad y excelencia tecnológica.",
    },
    {
      icon: "💎",
      title: "Valores",
      description:
        "Innovación continua, transparencia en los procesos, eficiencia operativa y compromiso con la excelencia en cada interacción con nuestros usuarios.",
    },
  ],
} = Astro.props;
---

<section id="nosotros" class="about">
  <div class="content-container">
    <div class="section-title">
      <h2>{sectionTitle}</h2>
      <p>{sectionSubtitle}</p>
    </div>
    <div class="about-grid">
      {
        cards.map((card) => (
          <div class="about-card">
            <div class="about-icon">{card.icon}</div>
            <h3>{card.title}</h3>
            <p>{card.description}</p>
          </div>
        ))
      }
    </div>
  </div>
</section>
