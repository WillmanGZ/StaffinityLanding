---
interface Props {
  icon: string;
  title: string;
  description: string;
}

const { icon, title, description } = Astro.props;
---

<div class="about-card">
  <div class="about-icon">{icon}</div>
  <h3>{title}</h3>
  <p>{description}</p>
</div>
